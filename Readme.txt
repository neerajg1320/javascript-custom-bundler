# https://www.youtube.com/watch?v=Gc9-7PBqOC8

The anonymous branch has the module function invocation using anonymous function with modules defined inline.

The flat branch define the moduleMap and bundlerFunction separately which makes the generated code more readable.